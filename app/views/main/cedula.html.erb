<% provide(:Votacion, "active") %>
<% provide(:Title, "Busqueda por cédula") %>

<div class="panel panel-default">
  	<div class="panel-heading">
    	<h3 class="panel-title">Busqueda de puestos de votación por cédula</h3>
  	</div>
  	<div class="panel-body">
	    <%= form_tag "/search/ocreate", method: :get, remote:  true, id:"search-form" do %>
			<%= text_field_tag :keyword, nil, placeholder: "Buscar cedula" %>
			<button type="submit" class="btn btn-default">Buscar</button>
		<%end%>

		<div id="resultados">
		</div>
  	</div>
</div>
